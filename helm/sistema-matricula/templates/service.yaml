apiVersion: v1
kind: Service
metadata:
  name: {{ include "sistema-matricula.fullname" . }}
  labels:
    {{- include "sistema-matricula.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sistema-matricula.selectorLabels" . | nindent 4 }}