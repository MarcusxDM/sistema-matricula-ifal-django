# Ambiente de Desenvolvimento
replicaCount: 1

image:
  tag: "develop-bca73b9"

service:
  type: NodePort

database:
  host: mysql-dev
  name: sistema_matricula_dev
  user: dev_user
  password: dev_password_123
  rootPassword: dev_root_password_123

ingress:
  enabled: true
  hosts:
    - host: dev.sistema-matricula.local
      paths:
        - path: /
          pathType: Prefix